language: python

python:
  - "2.7"

# command to install dependencies
install:
  - pip install coveralls
  - pip install -r requirements.txt

# command to run tests
script:
  - coverage run --source reportgen setup.py test

after_success:
    - coveralls
