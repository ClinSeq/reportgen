language: python

python:
  - "2.7"

# command to install dependencies
install:
  - pip install coveralls
  - pip install -r requirements.txt

# command to run tests
script:
  - py.test tests --cov coveralls

after_success:
    - coveralls
