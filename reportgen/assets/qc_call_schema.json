{
    "$schema": "http://json-schema.org/",
    "title": "QC schema",
    "type": "object",
    "properties": {
        "CALL": {
            "type": "string",
            "enum": ["OK", "WARN", "FAIL"]
        }
    }
}